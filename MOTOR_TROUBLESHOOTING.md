# ESP32-CAM 小车电机故障排除指南

## 📋 问题描述
- 遥控小车前后左右移动时，电机不转
- 发出滴滴声（电机驱动板欠压报警）
- 摄像头视频流正常工作

---

## ✅ 已完成的软件修复

### 1. LEDC 通道冲突 (已修复)
**问题：** 摄像头使用 `LEDC_CHANNEL_0`，电机 PWM 也使用通道 0-3，导致冲突。

**修复：** 电机 PWM 改用通道 4-7。

### 2. PWM 初始化顺序 (已修复)
**问题：** 电机 PWM 在摄像头初始化之前配置，被摄像头初始化覆盖。

**修复：** 电机 PWM 初始化移到 `esp_camera_init()` 之后。

### 3. 调试日志 (已添加)
在 `setMotor()` 和 `setOneMotor()` 函数中添加了串口输出，可以通过串口监视器观察命令是否被执行。

---

## 🔍 硬件排错步骤

### 步骤 1: 检查串口日志
1. 烧录代码后打开串口监视器 (115200 波特率)
2. 在手机上点击前进/后退按钮
3. 观察串口输出：
   ```
   [Motor] L=100, R=100
   [PWM] chF=4, chB=5, val=100, pwm=255
   [PWM] chF=6, chB=7, val=100, pwm=255
   ```
4. 如果没有看到这些输出，说明命令没有被接收

### 步骤 2: 检查电源
| 症状 | 可能原因 | 解决方法 |
|------|---------|---------|
| 滴滴声 + 电机不转 | 电机驱动板欠压 | 使用独立电池供电 |
| 电机抖动但不转 | 电流不足 | 换用 2A+ 电源 |
| 完全无反应 | 电源未连接 | 检查接线 |

**重要：** 电机驱动板应使用 **独立电源**（如 7.4V 锂电池），不要与 ESP32 共用 USB 供电！

### 步骤 3: 测量 GPIO 输出
用万用表测量以下 GPIO 在按前进时的电压变化：

| GPIO | 引脚 | 按前进时应该 |
|------|------|------------|
| GPIO 14 | gpLf (左前进) | 3.3V |
| GPIO 2 | gpLb (左后退) | 0V |
| GPIO 13 | gpRf (右前进) | 3.3V |
| GPIO 15 | gpRb (右后退) | 0V |

**如果电压正确但电机不转 → 硬件问题（驱动板/电机）**
**如果电压始终为 0V → 软件问题或 GPIO 冲突**

### 步骤 4: GPIO 2 特殊问题
⚠️ **GPIO 2 是 ESP32 的启动敏感引脚！**

- GPIO 2 在启动时必须为 **低电平**
- 如果电机驱动板在上电时给 GPIO 2 拉高，可能导致 ESP32 无法启动

**解决方案：** 考虑将 `gpLb` 从 GPIO 2 换到其他引脚（如 GPIO 12）

---

## 🔧 可选：更换 GPIO 引脚

如果 GPIO 2 确实有问题，可以修改 `ESP32CAM_Car.ino` 中的引脚定义：

```cpp
// 原始定义
int gpLb = 2;   // 可能有冲突

// 建议修改为
int gpLb = 12;  // 更安全的引脚
```

**AI-THINKER ESP32-CAM 可用引脚：**
- GPIO 12, 13, 14, 15 - 可用于电机
- GPIO 2, 4 - 有冲突风险（LED/Flash）
- GPIO 0 - 启动引脚，不建议使用

---

## 📊 电机驱动板检查清单

- [ ] 驱动板电源指示灯亮
- [ ] 驱动板输入电压 ≥ 6V
- [ ] 使能端 (EN) 接高电平或 PWM
- [ ] GND 与 ESP32 共地
- [ ] IN1-IN4 连接到正确的 GPIO

---

## 🧪 测试命令

在浏览器地址栏输入以下 URL 直接测试电机：

```
前进: http://192.168.1.100/go
后退: http://192.168.1.100/back
左转: http://192.168.1.100/left
右转: http://192.168.1.100/right
停止: http://192.168.1.100/stop
```

同时观察串口输出，确认命令被执行。

---

## 📝 问题汇报信息

如需进一步帮助，请提供：
1. 串口监视器输出截图
2. 电机驱动板型号 (L298N / TB6612 / 其他)
3. 电源配置（电压、电流容量）
4. 接线照片
